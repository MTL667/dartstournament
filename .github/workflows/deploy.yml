name: Deploy to Easypanel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Trigger Easypanel deployment
        run: |
          echo "Push to main branch detected. Easypanel will auto-deploy if connected to GitHub."

